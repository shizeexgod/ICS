meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12
build:
  # Здесь должно быть пусто или указан только путь к requirements.txt
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 80